const colors = ["Red", "Green", "White", "Black"]

const comma = colors.toString()

const commaSpace = colors.join(", ")

const plus = colors.join(" + ")

console.log(comma)
console.log(commaSpace)
console.log(plus)